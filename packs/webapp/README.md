# Helm Webapp Pack

This sample pack extends the default Helm pack with generic full-stack web delivery context.

Install it with:

```bash
node dist/cli.js install-agent --target <repo> --pack webapp --run-baseline
```